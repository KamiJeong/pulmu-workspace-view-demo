# Demo repository instructions

Keep this repository intentionally small.
Use Node's built-in test runner; do not add dependencies for the Pulmu demo task.
Preserve the existing TaskStore API unless the requested task explicitly changes it.
