{
  "name": "@pulmu/storybook",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "scripts": {
    "storybook": "storybook dev --host 127.0.0.1 --port 6006",
    "build-storybook": "storybook build --output-dir ../../storybook-static"
  },
  "dependencies": {
    "@pulmu/icons": "workspace:*",
    "@pulmu/model": "workspace:*",
    "@pulmu/tokens": "workspace:*",
    "@pulmu/ui": "workspace:*"
  }
}
